/**
 * @name generateSeed
 * @summary Generate a new account
 * @returns An object with the seed and the public address
 */
export declare const generateSeed: () => Promise<{
    seed: string;
    address: string;
}>;
/**
 * @name getKeyringFromSeed
 * @summary Create a keyring from a seed
 * @param seed
 * @returns A keyring pair
 */
export declare const getKeyringFromSeed: (seed: string) => Promise<import("@polkadot/keyring/types").KeyringPair>;
